generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:/home/vitorandrey/.config/price-reader/sqlite/database.db"
}

model Product {
  id    String @id @unique
  name  String
  price Float

  @@map("products")
}
